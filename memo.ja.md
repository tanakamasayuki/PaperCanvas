# PaperCanvas

**概念・要件仕様書**

**レシート／ラベル向けモノクロビットマップ生成ライブラリ**

> **中心コンセプト — 印刷内容の配置と描画に専念し、プリンター通信や機種固有制御から独立した、予測可能な1bitビットマップ生成基盤とする。**

| 文書種別 | 概念・要件仕様書 |
| --- | --- |
| 対象 | 組み込み機器向け小型プリンター |
| 版 | Draft 0.1 |

2026-08-03

## 1. 文書の目的

本書は、PaperCanvasの実装方法ではなく、ライブラリが提供する価値、責務、対象範囲、利用モデル、および初期要件を定義する。

- レシートおよびラベルの描画モデルを整理する。
- ライブラリ本体とプリンタードライバー／BLE通信の責務境界を明確にする。
- 初期リリースで必要な機能と、意図的に対象外とする機能を定める。

## 2. プロダクト概要

PaperCanvasは、テキストと画像を指定された印刷領域へ描画し、最終的にモノクロの1bitビットマップデータを生成するライブラリである。描画エンジンとしてLovyanGFXを利用してよいが、LovyanGFX自体を製品価値の中心には置かない。

> **ライブラリの責務 — 紙面サイズと描画指示を受け取り、プリンターへ渡せる共通形式のモノクロビットマップを生成するところまで。**

## 3. 設計原則

| 原則 | 内容 |
| --- | --- |
| 責務を小さく保つ | 接続、通信、再送、機種固有コマンドは扱わない。 |
| 結果を予測可能にする | 同じ入力と設定から、常に同じビットマップを生成する。 |
| 印刷物の種類で利用モデルを分ける | レシートは縦積み、ラベルは固定キャンバス上の矩形配置とする。 |
| 外部生成物を受け入れる | バーコードなどは画像データとして受け取れるようにする。 |
| コアを汎用に保つ | 特定のプリンター、BLE仕様、バーコード規格に依存しない。 |

## 4. システム境界

PaperCanvasは、印刷システム全体のうち「紙面レイアウトとラスター生成」のみを担当する。

| 1 | アプリケーションが印刷内容を用意する |
| --- | --- |
| 2 | PaperCanvasがレイアウトし、1bitビットマップを生成する |
| 3 | 別ライブラリが機種固有の印刷データへ変換する |
| 4 | BLE等の通信層がプリンターへ送信する |

## 5. 対象となる印刷物

### 5.1 レシート

横幅が固定され、内容に応じて縦方向へ伸びる印刷物として扱う。利用者は印刷要素を上から順に追加する。

- 最初に横幅情報を設定する。
- テキストまたは画像を上から順に追加する。
- 各要素の高さを積み上げ、最終的な紙面高さを決定する。
- 原則として内容は欠落させず、必要な分だけ縦へ伸ばす。

### 5.2 ラベル

幅、高さ、解像度から確定する固定キャンバスとして扱う。自動レイアウトはコア機能とせず、利用者が各要素の矩形を指定する。

- 各要素は位置と大きさを持つ矩形として定義する。
- 矩形内での寄せ方、フィット方法、クリップ方法を指定する。
- レイアウト作業はラベル専用ブラウザツールで補助する。

## 6. 入力要件

### 6.1 共通入力

| 項目 | 要件 |
| --- | --- |
| 解像度 | 物理サイズとピクセル数の対応に使用する。 |
| 背景 | 初期状態は白。必要に応じて白黒極性を指定可能とする。 |
| 描画要素 | 初期対象はテキストと画像。 |
| 余白 | 紙面または要素単位で指定できることが望ましい。 |

### 6.2 レシート固有入力

- 印字可能な横幅、または横幅を算出するための物理幅と解像度。
- 上から順に追加される描画要素。
- 要素間の間隔および上下余白。

### 6.3 ラベル固有入力

- ラベルの物理幅、物理高さ、解像度、または確定済みピクセルサイズ。
- 各描画要素の矩形。
- 矩形内での描画ルール。

## 7. 描画要素の要件

### 7.1 画像

画像は、外部で用意された画像データを受け取り、指定領域へ配置する。バーコードやQRコードも、初期段階では画像として扱える。

- 左寄せ、中央、右寄せなどの配置指定。
- 原寸、倍率指定、領域に合わせる等のスケール指定。
- 縦横比を維持するかどうか。
- 領域を超える場合の縮小またはクリップ。
- モノクロ化方法の指定または既定処理。

### 7.2 テキスト

テキストは、フォントと文字サイズを基準に描画する。自動折り返しは便利である一方、印刷結果を予測しづらくするため、初期要件では保守的に扱う。

- フォント種類。
- 文字サイズまたは文字高さ。
- 左寄せ、中央、右寄せ。
- 明示改行の保持。
- 必要に応じて単純折り返しを選択可能とする。
- 矩形を超える場合はクリップまたはエラー／警告とする。

## 8. バーコードの扱い

PaperCanvas自体は、バーコード文字列から規格に応じたバー列やモジュール列を生成する責務を持たない。

- バーコード生成専用ライブラリへ依存し、その出力を画像として受け取る。
- または、利用者が事前に生成した画像データを渡す。
- PaperCanvasは受け取った画像の配置、拡大縮小、クリップ、モノクロ出力のみを担当する。

> **理由 — バーコード規格、チェックデジット、最小モジュール幅、誤り訂正などの責務を分離し、PaperCanvasを小さく保つため。**

## 9. 出力要件

出力は、プリンター機種に依存しない共通形式のモノクロビットマップとする。

| 項目 | 内容 |
| --- | --- |
| 幅 | ピクセル単位のキャンバス幅。 |
| 高さ | ピクセル単位のキャンバス高さ。レシートでは内容から確定する。 |
| 画素データ | 1画素1bitの白黒データ。 |
| 行情報 | 1行あたりのバイト数を判別可能であること。 |
| 極性 | 0/1のどちらを黒とするか明確であること。 |

ビット順、行末パディング、コマンド形式など、機種依存の変換は別ライブラリの責務とする。

## 10. ラベル用ブラウザツール

矩形指定を手作業で記述する負担を軽減するため、PaperCanvasとは別にラベル専用のブラウザツールを用意する。既存の画面レイアウトツールとは用途が異なるため、製品としては分離する。

- ラベル幅、高さ、解像度の設定。
- テキスト矩形および画像矩形の追加。
- ドラッグ移動とリサイズ。
- 寄せ方、スケール、フォントなどの設定。
- モノクロプレビュー。
- JSON、設定データ、またはC++コード例の生成。
- プリンターへの直接印刷は補助機能として実装可能だが、PaperCanvas本体の要件には含めない。

## 11. 非機能要件

- 同じ入力から同じ出力を生成する決定性。
- 組み込み環境で利用できる小さな依存関係と明確な責務。
- プリンター機種や通信方式に依存しないこと。
- メモリ制約を考慮し、将来的に分割生成やストリーム出力へ拡張可能であること。
- 異常なサイズ指定や領域外描画を検出できること。

## 12. 初期リリースの対象範囲

1. レシートの固定幅・可変高さキャンバス。
1. レシートへのテキストおよび画像の縦積み。
1. ラベルの固定幅・固定高さキャンバス。
1. ラベル上の矩形ベースによるテキストおよび画像配置。
1. 画像の基本的な寄せ方とスケール。
1. テキストのフォント、サイズ、寄せ方、明示改行。
1. モノクロ化と1bitビットマップ出力。
1. ラベル専用ブラウザツール向けの保存／生成可能なレイアウト定義。

## 13. 初期リリースで対象外とするもの

- BLE接続、サービス探索、再接続、データ送信。
- ESC/POSやメーカー独自コマンドの生成。
- 印刷ジョブ管理、再送、進捗管理。
- バーコードやQRコードのエンコード処理。
- ラベルの完全自動レイアウト。
- 複雑な組版、CSS互換レイアウト、DTP機能。
- GUI画面向けの部品配置やイベント処理。

## 14. 成功条件

- 利用者がプリンター通信を意識せず、印刷内容から1bitビットマップを生成できる。
- レシートは、指定幅に対してテキストと画像を上から順に積み上げられる。
- ラベルは、指定矩形とブラウザツールを使って狙った配置を再現できる。
- バーコード生成やBLE通信を別の部品へ置き換えても、PaperCanvas本体を変更せず利用できる。
- ライブラリの責務が「紙面生成」に留まり、用途拡張によって複雑化しすぎない。

## 15. 今後の検討事項

| 論点 | 検討内容 |
| --- | --- |
| 折り返し | 単純折り返しを初期機能に含めるか、明示改行のみから始めるか。 |
| モノクロ化 | 画像ごとに閾値やディザ方式を指定可能にするか。 |
| 単位 | 内部指定をピクセル中心にするか、mm指定も第一級に扱うか。 |
| メモリ | レシート全体を保持する方式と、帯状に生成する方式の優先順位。 |
| レイアウト定義 | ブラウザツールと組み込み側で共有する中間形式をJSON等で定義するか。 |
| 診断 | 領域外、文字切れ、画像縮小などを警告として返すか。 |
